# Terms

CompText Guard 0.1.x is an experimental local policy plugin for safer autonomous repository work.

Hook allow/deny/warn decisions are narrow policy decisions, not proof that an allowed action is secure or correct. Users must review and trust plugin hooks before activation.

Do not bypass a guard decision silently; change policy only within an explicitly authorized scope.
